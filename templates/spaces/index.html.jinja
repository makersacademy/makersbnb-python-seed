<!DOCTYPE html>
<html>
    <head>
        <title>Spaces</title>

        <!--
            Head to https://github.com/makersacademy/web-applications-in-python/blob/main/pills/loading_css_or_static_files.md
            If you're wondering how the CSS file below is being loaded.
        -->
        <link rel="stylesheet" href="/static/style.css" >
    </head>
    <body>
        {{ render_partial('shared/partials/navbar.html.jinja')}}
        <h1>Spaces available.</h1>
        {% for space in spaces %}
        <div class="space-div">
            <a href="{{space.id}}">{{space.space_name}}</a>
            <li class="space-id">id: {{space.id}}</li>
            <li>
                Description: {{space.description}} 
            </li>
            <li>
                Price Per Night: {{space.price_per_night}} 
            </li>
            <li>
                User ID: {{space.user_id}} 
            </li>
            <br>
        </div>
        {% endfor %}
    </body>
</html>